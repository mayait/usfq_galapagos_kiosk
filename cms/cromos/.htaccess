# URLs lindas por cromo: /cromos/<slug> → cromo.php?slug=<slug>
RewriteEngine On
RewriteBase /cromos/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([a-z0-9_]+)/?$ cromo.php?slug=$1 [L,QSA]
